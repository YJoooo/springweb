1. 외부 객체 생성지 멤버(구성요소) 관련 에러 발생
	==>
	클래스를 만들고 마지막에는 ctrl+s로 반드시 저장하여야, 실행할 수 있는 컴파일(.class)
	파일이 생성된다. 외부에서 호출할 떄, 클래스 구성요소가 에러가 나면 이부분을 반드시 점검하세요.
# front단 에러 처리(js)
0. 웹서버 console에는 에러가 없고,
1. 이벤트 작동(클릭/submit등)이 되지 않을때. ==> 자바스크립트 에러!
2. f12 에러라인
	- 해당 라인의 위쪽 라인과 아래쪽 라인
		if(조건){ : 중괄호, 소괄호 블럭을 빠트린 경우
		==> 습관적으로 중괄호 소괄호를 시작과 닫기를 먼저하고 진행
		if(){
		
		}
# backend단 에러
1. console 창에서 확인 db 에러 sqlSession ==> mapper sql에러
2. property 처리 값 처리 getXXX(), setXXX()
3. 404에러 대부분은 
	controller 단에 url mapping
	주로 요청값이 type과 메서드명(property)가 일치하지 않을 때.












			